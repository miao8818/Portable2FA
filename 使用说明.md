# Portable 2FA 使用说明

当前版本号和更新时间以项目根目录的 `version.json` 为准，并会显示在软件界面右上角。

## 使用方法

1. 双击项目根目录中的 `Portable2FA.exe`。
2. 粘贴 Base32 密钥或 `otpauth://` 链接。
3. 点击验证码或“复制验证码”按钮即可复制。
4. 关闭或最小化窗口后，程序会留在系统托盘。
5. 双击托盘图标恢复窗口；右键托盘图标可复制验证码或退出程序。

## 支持格式

- Base32 密钥，允许空格、连字符和末尾填充符。
- `otpauth://totp/...` 链接。
- SHA1、SHA256、SHA512。
- 6 位或 8 位验证码。
- 5 至 300 秒周期。

## 隐私

程序不联网，也不会把密钥写入磁盘。退出程序后，密钥随进程内存一同释放。

## 重新构建

```powershell
.\build.ps1 -OutputDirectory .
```

构建脚本使用 Windows 自带的 C# 编译器，并自动运行 RFC 6238、Base32 和
`otpauth://` 解析测试。

项目主页：<https://github.com/miao8818/Portable2FA>
