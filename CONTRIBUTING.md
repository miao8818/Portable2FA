# 参与贡献

## 开发约定

- TOTP、二维码识别、账户存储和截图处理默认在本地完成。
- 不加入自建服务端、遥测、广告或分析 SDK。
- 本地密钥持久化必须继续使用 DPAPI；跨设备同步限定为用户主动启用的 Windows
  Credential Locker。
- 不在日志、测试、截图、Issue 或提交记录中包含真实 2FA 密钥。
- TOTP、二维码、账户序列化或同步合并逻辑变更必须同步增加回归检查。
- UI 修改需在 100%、125% 和 150% DPI 下检查主窗口与弹窗。
- 长标签、长邮箱和长服务名必须保持在容器内；列表可省略显示，但需提供完整查看路径。

## 提交前检查

```powershell
.\build.ps1 -OutputDirectory .
```

构建必须输出 `PASS: 27 checks`，并至少验证：

1. Base32、`otpauth://` 和测试二维码能生成预期验证码。
2. 文件、剪贴板和截图三种二维码入口可用。
3. 账户保存、更新、删除和 DPAPI 重载可用。
4. 长邮箱标签不会覆盖列表内容，悬停可查看完整文本。
5. 自定义唤起/截图快捷键可以注册并触发。
6. `--startup` 启动时无可见窗口且可从托盘恢复。
7. Windows Credential Locker 可用性检查不会创建测试账户。
8. 2560×1600、150% 缩放下页面和弹窗内容完整。

最后运行：

```powershell
git diff --check
git status --short
```

## 强制版本流程

每次对外发布代码、功能、修复或文档更新都必须更新版本号和更新时间：

```powershell
.\update-version.ps1 -Part patch
```

- `patch`：修复或文档更新。
- `minor`：向后兼容的新功能。
- `major`：不兼容变更。

发布步骤：

1. 检查 `version.json` 中的语义化版本和 ISO 8601 时间。
2. 在 `CHANGELOG.md` 顶部添加对应版本记录。
3. 重新生成 README 功能截图，确保只使用合成测试数据。
4. 运行完整构建与 27 项检查。
5. 生成便携 EXE 和 `SHA256SUMS.txt`。
6. 提交代码，创建 `v版本号` Git 标签并推送。
7. 创建 GitHub Release，上传 EXE 与校验文件并验证公开下载。

## Pull Request

请保持一次 PR 只处理一个明确主题，在说明中列出行为变化、数据迁移影响、DPI 验证和测试
结果。引入第三方二进制时必须提交许可证、来源、固定版本和 SHA-256。
